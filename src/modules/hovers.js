// Index-list hover previews + link hovers (desktop only).
// Phase 5: paste the chosen Osmo Vault hover component here and wire it to
// [data-hover] attributes set in Webflow.
export function initHovers(container) {
  const targets = container.querySelectorAll('[data-hover]')
  if (!targets.length) return
  // TODO Phase 5: Osmo hover-preview implementation
}
